pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                echo 'O Jenkins baixou o código automaticamente!'
                sh 'ls -la'
            }
        }
        stage('Build') {
            steps {
                echo 'Simulando build...'
            }
        }
    }
}