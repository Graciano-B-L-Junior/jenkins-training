pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                echo 'Build disparado pelo Git! pt 3'
                sh 'ls -la'
            }
        }
        stage('Build') {
            steps {
                echo 'Simulando build...'
            }
        }
    }
}